Test Images

This folder contains a few tiny PPM fixtures for exercising the `/detect` endpoint.

The benchmark script uses these files by default, but you can point it at a different folder with `-d` or `IMAGE_DIR`.
